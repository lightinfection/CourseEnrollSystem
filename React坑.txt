react jsx中，class => classname， <label>中的for => htmlFor，Array.map(()=>{})中，
{}会被JSX识别为html部分，可将{}更改成()，或者在{}中加入return(<div>……</div>)
jsx中循环只能使用.map(),不需要转换成jscode的话不需要加{}，直接.map((d)=> <div>{d.prop}</div>)即可
JS Array.sort(), Date.parse(i)将标准时间i转化为数值
react中props的变化不会让模块重新渲染，只有state的变化才会使模块重新渲染

req.params {:_id: , :_ip: ,}
req.body {a: , b: , }

JWT声明中，JWT后要加空格分隔符，如"JWT "+toekn

React Hook "useState" cannot be called in a class component. React Hooks must be called in a React function component or a custom React Hook function